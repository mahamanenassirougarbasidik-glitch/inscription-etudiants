from setuptools import setup, find_packages

setup(
    name="inscription_etudiants",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Les dépendances sont déjà dans requirements.txt
        # On peut les laisser vides ou les lister, mais on utilisera requirements.txt
    ],
)