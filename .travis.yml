language: python
python:
  - "3.8"
  - "3.9"
  - "3.10"
install:
  - pip install -r requirements.txt
script:
  - pytest --cov=src
after_success:
  # Optionnel : envoi du rapport de couverture vers Coveralls
  - pip install coveralls
  - coveralls